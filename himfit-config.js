// Sign-in screen: only appears on the live site when BOTH values below are filled and this file is deployed.
// (Empty keys = full app opens without cloud sign-in.) Guide: docs/SUPABASE_FULL_SETUP.md
window.HIMFIT_SUPABASE_URL = '';
window.HIMFIT_SUPABASE_ANON_KEY = '';

// Strava embed (Runs): only this Supabase auth user id can turn it on (Dashboard → Authentication → Users → copy UUID).
// Leave empty to hide the toggle for everyone and keep the embed off.
window.HIMFIT_STRAVA_OWNER_USER_ID = '';
